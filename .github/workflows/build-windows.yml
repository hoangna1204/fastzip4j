name: Build Windows

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    uses: ./.github/workflows/build-go-lib.yml
    with:
      target-os: 'windows'
      runner: 'windows-latest'
      output-file: 'libfastzip4j.dll'
      cc-compiler: ''
      shell-type: 'pwsh'